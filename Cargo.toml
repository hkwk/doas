[package]
name = "doas"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
regex = "1.10"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
pam = "0.7"
